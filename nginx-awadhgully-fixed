server {
    server_name awadhgully.com www.awadhgully.com;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /shop-api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /admin-api {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Added Health Check Block
    location /health {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /assets {
        alias /var/www/awadhgully/vendure/assets;
    }

    # Admin UI
    location /admin {
        alias /var/www/awadhgully/vendure/packages/admin-ui/dist/browser;
        try_files $uri $uri/ /admin/index.html;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/awadhgully.com/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/awadhgully.com/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = www.awadhgully.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = awadhgully.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    listen 80;
    server_name awadhgully.com www.awadhgully.com;
    return 404; # managed by Certbot
}
